import React from 'react'
import Layout from './Layout/Layout'

const NotFound = () => {
    return (
        <Layout>
            <div>NotFound</div>
        </Layout>
    )
}

export default NotFound